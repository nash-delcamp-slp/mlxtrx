% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{job_in_sq}
\alias{job_in_sq}
\title{Check if job IDs are currently in the job queue}
\usage{
job_in_sq(job_id)
}
\arguments{
\item{job_id}{Numeric or character vector. Job IDs to check in the queue.}
}
\value{
Logical vector. Returns \code{TRUE} for job IDs found in the queue,
\code{FALSE} for job IDs not found or if the \code{sq} command fails.
}
\description{
Queries the job queue using the \code{sq} command to determine if one or more
job IDs are currently active (running or queued).
}
\details{
This function executes the \code{sq} command to retrieve current job queue
information and parses the output to check for the presence of specified
job IDs. The function:
\enumerate{
\item Runs \code{system2("sq")} to get queue status
\item Parses the output as a space-separated table with headers
\item Checks if job IDs exist in the \code{JOBID} column
}

If the \code{sq} command fails, returns no output, or the output cannot be
parsed, the function returns \code{FALSE} and issues appropriate warnings.
}
\examples{
\dontrun{
# Check single job ID
job_in_sq(12345)

# Check multiple job IDs
job_in_sq(c(12345, 67890))

job_in_sq(c("12345", "67890"))
}

}
\seealso{
\code{\link{monitor_jobs}} for monitoring job completion,
\code{\link{execute_job}} for submitting jobs
}
\keyword{internal}
